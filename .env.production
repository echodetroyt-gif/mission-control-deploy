# OpenClaw API configuration
# This should point to your OpenClaw gateway
# Options:
# 1. Use Tailscale IP (if both on same tailnet)
# 2. Use ngrok/localtunnel pointing to Pi (temporary)
# 3. Use a reverse proxy setup

OPENCLAW_API_URL=
OPENCLAW_TOKEN=
